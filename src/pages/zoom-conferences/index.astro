---
import Layout from '../../layouts/PageLayout.astro';
import ContentList from '../../components/numsa/ContentList.astro';
import Headline from '../../components/blog/Headline.astro';

import { blogListRobots } from '../../utils/blog';
import { getCollection } from 'astro:content';
import { formatContent } from '../../utils/numsa';

export const prerender = true;

// Get ALL zoom conferences from all administrations
const allPosts = await getCollection('zoom-conferences');
const formattedPosts = formatContent(allPosts, 'zoom-conferences');

const metadata = {
  title: 'Zoom Conferences - All Administrations',
  robots: {
    index: blogListRobots?.index,
    follow: blogListRobots?.follow,
  },
  openGraph: {
    type: 'numsa',
  },
};
---

<Layout metadata={metadata}>
  <section class="px-6 sm:px-6 py-12 sm:py-16 lg:py-20 mx-auto max-w-4xl">
    <Headline
      subtitle="A collection of all Zoom conferences by NUMSA across all administrations"
    >
        NUMSA ZOOM Conferences
    </Headline>
    <ContentList items={formattedPosts} contentType="zoom-conferences" />
  </section>
</Layout>
